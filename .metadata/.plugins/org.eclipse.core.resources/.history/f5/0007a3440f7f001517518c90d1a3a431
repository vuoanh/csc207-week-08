
public interface List<T>{
	void add(T variable);
	void remove(T variable);
	void remove(int index);
	void clear();
	T get(int index);
	void set(T variable, int index);
	int indexOf(T variable);
	boolean contains(T variable);
	boolean isEmpty();
	long size();
	
	
}
